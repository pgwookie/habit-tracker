# Habit Tracker (Django)

Учебный проект: трекер привычек на Django.

## Функции (на текущий момент)
- Регистрация / вход / выход
- Личный кабинет (username/email/дата регистрации)
- Редактирование профиля: full_name, bio, timezone (селект), язык по умолчанию
- Локализация RU/EN + переключатель языка в navbar


## Требования
- Python 3.9+
- (опционально) GNU gettext tools (msgfmt/msguniq) для переводов

## Установка и запуск (Windows)
1) Создать и активировать venv:
python -m venv .venv
.\.venv\Scripts\Activate.ps1

Установить зависимости:

pip install -r requirements.txt

Создать .env по примеру:

скопируй .env.example → .env

заполни SECRET_KEY

Применить миграции и запустить сервер:

.\.venv\Scripts\python.exe manage.py migrate
.\.venv\Scripts\python.exe manage.py runserver

Открыть:

http://127.0.0.1:8000/accounts/login/

http://127.0.0.1:8000/accounts/profile/

## Sprint 1 (16.02–02.03)

### Задачи спринта
- [x] **[4] Регистрация + авторизация пользователя**  
- Создать страницу с формой регистрации
- Реализовать валидацию (совпадение паролей, уникальность email). 
- При успехе – автоматический вход и редирект в личный кабинет.
- Страница входа (email, пароль). 
- Проверка учётных данных, установка сессии. 
- При ошибке – сообщение. 
- Реализовать выход (logout) с перенаправлением на главную

Ожидаемый результат:Новая учётная запись сохраняется в БД, пользователь попадает в личный кабинет. Пользователь может войти и выйти из системы.

Итог: реализованы регистрация, вход и выход; настроены редиректы и валидация ошибок.


- [x] **[4] Личный кабинет пользователя**  
- Создать страницу профиля пользователя. 
- Отображать основную информацию: username, email, дата регистрации. 
- Реализовать редактирование полей профиля: full_name, bio (о себе). 
- Реализовать настройку часового пояса (timezone) через удобный выпадающий список (город + UTC± + текущее время). 
- Реализовать выбор языка по умолчанию (RU/EN) и применение после сохранения.

Ожидаемый результат: пользователь видит и может обновлять данные профиля; настройки сохраняются в БД; интерфейс профиля удобный и понятный.

Итог: профиль реализован и расширен; добавлены дополнительные поля и настройки; реализован удобный выбор таймзоны.


- [x] **[3] Переключение языка интерфейса RU/EN**  
- Настроить локализацию Django (LocaleMiddleware, LANGUAGES, LOCALE_PATHS). 
- Добавить переключатель языка в navbar (RU/EN). 
- Перевести основные элементы интерфейса (login/register/profile, кнопки и подписи). 
- Сгенерировать и заполнить django.po, собрать переводы в django.mo. 
- Проверить, что при переключении языка переводятся все ключевые элементы страниц.

Ожидаемый результат: пользователь может переключать язык интерфейса между RU/EN, страницы отображаются корректно на выбранном языке.

Итог: локализация настроена, переключатель языка работает, переводы добавлены и собраны.
